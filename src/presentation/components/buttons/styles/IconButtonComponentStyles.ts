import {StyleSheet} from 'react-native';

const IconButtonComponentStyles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'stretch',
  },
});

export default IconButtonComponentStyles;
