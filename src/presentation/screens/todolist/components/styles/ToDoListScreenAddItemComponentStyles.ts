import {StyleSheet} from 'react-native';

const ToDoListScreenAddItemComponentStyles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'stretch',
    justifyContent: 'center',
  },
  contentContainer: {
    marginHorizontal: 20,
  },
});

export default ToDoListScreenAddItemComponentStyles;
