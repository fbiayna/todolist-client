import {StyleSheet} from 'react-native';

const AuthenticationScreenPresenterStyles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'stretch',
  },
});

export default AuthenticationScreenPresenterStyles;
